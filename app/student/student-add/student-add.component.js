import template from './student-add.html';
import controller from './student-add.controller';

const StudentAddComponent = {
    restrict: 'E',
    bindings: {},
    template,
    controller
};

export default StudentAddComponent;