import template from './course-add.html';
import controller from './course-add.controller';

const CourseAddComponent = {
    restrict: 'E',
    bindings: {
    },
    template,
    controller
};

export default CourseAddComponent;